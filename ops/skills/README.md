# Agent skills (source)

Anonymised skill markdown used on the portfolio page:

- `xenrich` — embedded under **Closing the loop**
- `autopilot` + `log` — embedded under **Autopilot**

Copy from the page (Copy markdown). These folders are the source files only — not a separate public section.
